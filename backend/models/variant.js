const mongoose = require("mongoose");

const Variants = mongoose.model("Variants", {
  name: {
    type: String,
    required: true,
  },
  price: {
    type: Number,
    required: true,
  },
  category: {
    type: String,
    required: true,
  },
});

module.exports = Variants;
